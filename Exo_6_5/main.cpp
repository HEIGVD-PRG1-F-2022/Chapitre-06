// Fichier pour l'exercices 5 du chapitre 6.
#include <iomanip>
#include <iostream>
#include <string>

using namespace std;

int main() {
  string str = "123";
  // La meilleure formulation
  if (str.empty()) {
  }
  if (str == "") {
  }
  if (str.length() == 0) {
  }
  if (str.size() == 0) {
  }
  return 0;
}